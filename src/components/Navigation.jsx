import "../App.css";

function Navigation() {
  return (
    <nav class="navbar fixed-top bg-body-tertiary navigation-container">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">
          Fixed top
        </a>
      </div>
    </nav>
  );
}

export default Navigation;
